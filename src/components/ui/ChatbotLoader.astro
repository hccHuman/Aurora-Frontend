---
/**
 * ChatbotLoader Astro Component
 *
 * Lazy-loads the ChatWrapper component to reduce initial bundle size.
 * The chatbot module is code-split and loaded on-demand, significantly
 * reducing the main bundle footprint (~1MB → ~200-300KB in main bundle).
 */
import "@/styles/global.css";
import "@/styles/Components/ChatFrame.css";
import ChatWrapperLazy from "@/modules/AURORA/components/ChatWrapperLazy";
import { YoliAnnouncer } from "@/modules/YOLI/components/YoliAnnouncer";
---

<div
  id="aurora-global-container"
  transition:persist
  transition:name="aurora-chat"
  class="relative z-[9999]"
>
  <!-- Y.O.L.I. ARIA Announcer (Hidden) -->
  <YoliAnnouncer client:load />

  <div class="flex justify-center">
    <!-- Lazy-loaded ChatWrapper: code-split to separate bundle -->
    <ChatWrapperLazy client:only="react" />
  </div>
</div>
