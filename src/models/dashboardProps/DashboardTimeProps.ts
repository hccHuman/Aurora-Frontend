/**
 * Dashboard Statistics Range Type
 * 
 * Defines the supported time intervals for dashboard data visualization
 * (e.g., today, last week, last month, etc.).
 */
export type StatsRange = "today" | "7d" | "30d" | "90d" | "1y";